import Body from "@/components/body";

const DashboardPage = () => {
    return (
        <main className="mt-[100px] bg-green-500">
            <Body />
        </main>
    );
};

export default DashboardPage;
